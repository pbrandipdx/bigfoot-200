# Copy to tracker/.env and fill in. tracker/.env is gitignored - never commit it.
GARMIN_EMAIL=you@example.com
GARMIN_PASSWORD=                # only for the first run; a token is cached to ~/.garminconnect after
SUPABASE_URL=https://mwjprmuwdhfjlldrruhx.supabase.co
SUPABASE_SERVICE_KEY=           # Supabase dashboard > Project Settings > API > service_role
