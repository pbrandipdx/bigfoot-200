# Generated — do not hand-edit

Both files here are built from `tracker/weeks.json` by `make`.
Any edit you make by hand is erased on the next build.

If git ever reports a conflict in this folder, don't resolve it by hand:

    git checkout --theirs build/ && make

The real content lives in `tracker/weeks.json`.
